# 术语表

- `webshell`: 当前业务模块名称（文件夹维度）
- `project_id`: 项目标识，建议统一来自 `row.path`（无 path 时用 `row.value`）
- `workspacePanelList`: 工作区分组状态（顶层横向分组）
- `row`: 当前 list/tabs 行上下文对象（由 `_target_row` 映射）
- `loadedMap`: 已加载 tab 标记映射（点击项目时动态扩展）
- `activeKey`: 当前激活 tab key
