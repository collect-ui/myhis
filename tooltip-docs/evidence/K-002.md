# K-002

## 作用

- 记录低代码文档抽取与改写的证据，便于追溯与复核。


## 关键摘录（页面展示内容）
- 示例代码首行：`- key: bulk_create_user`
- 说明：参数就是前台过来的参数，参数只能是json对象，里面有个service+一些其他业务参数。
    示例中params 是一个参数池，参数是可以http 请求传来，也可以自己定义，主要存储一些key，value 字典对象，handler_params 和result_handler 可以对参数进行处理。
请求过来的参数，也是直接设置到params,param
- 列表要点：定义服务的params参数，与参数的简单校验
- 列表要点：参数优先级template >你传的>default 。最后运行check
- 列表要点：每个默认有参数session_user_id，当前登陆用户ID

## 改写策略
- 保留原文核心语义。
- 统一整理成 tooltip 结构：作用、参数、示例、说明要点。

## 元信息

- 时间：2026-05-07 16:21:59
- 读取方式：无头浏览器点击左侧目录树
- 源分组：如何编写服务
- 源文档：`什么是参数`
- 页面标题：`什么是参数(服务描述)`
- 目标文件：`key/params.md`
- 截图：`/data/project/sport/tooltip-docs/evidence/K-002-source.png`
- 原始读取：`/data/project/sport/tooltip-docs/evidence/K-002-source.json`
