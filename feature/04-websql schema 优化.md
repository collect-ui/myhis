# 现象
目前点击schema 回刷新现有数据有哪些表，哪些字段，展示有点不好看
# 要求
- 要求以树的形式展示，类似navcat 有哪些表、点击表展示字段，字段上显示类型，varchar 形式
- 显示schema ,表、视图、索引，然后具体表-> 字段 
- 这个数据比较大，希望用虚拟树展示
- 类似小熊dbbeaver
- 支持搜索过滤，过滤表名称、过滤表，字段
- 支持右键查看ddl,看表结构，视图的sql
# 测试要求
- 用无头浏览器测试，测试表名、字段
- 分辨验证结果，修复